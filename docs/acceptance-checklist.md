# 阶段验收清单

本文件是当前唯一进度表，由总负责人在实测后维护。状态只使用：`未开始`、`进行中`、`待验收`、`通过`、`退回`、`阻塞`。

## 当前状态

| 阶段 | 状态 | 日期 | 结论 |
|---|---|---|---|
| D0-D5 | 通过 | 2026-08-10 | `data-v1` 已冻结；203 类，221,377 张可用图片，固定 split 无路径或重复组泄漏 |
| A1 | 通过 | 2026-08-10 | preflight、Dataset、预处理、ResNet-50 冒烟、checkpoint 和小样本过拟合均通过 |
| A2 | 通过 | 2026-08-11 | ResNet-50 普通 CE 与 weighted CE 完成；按 val Macro-F1 选择 weighted CE |
| A3 | 通过 | 2026-08-11 | weighted CE epoch 21 已冻结；test 仅评估一次，模型包及一次性消费凭据完整 |
| P1 | 通过 | 2026-08-10 | Predictor、模型包契约、异常图片处理、假模型页面及响应式浏览器验收通过 |
| P2 | 通过 | 2026-08-11 | 真实 ResNet-50 已接入；固定 val、CPU/CUDA、异常输入和低置信度提示通过 |
| F0 | 通过 | 2026-08-11 | 版本链、复现命令、Git 范围、三条演示路径和最终文档全部通过 |
| T0 | 通过 | 2026-08-12 | IP102 检测数据合同、固定 train/val/test、Dataset 全量遍历和 20 项 checksum 通过 |
| J1 | 通过 | 2026-08-12 | 历史分类权重已适配统一 224 直缩预处理；val Macro-F1 选择 epoch 5，Top-1 门禁通过 |
| J2 | 通过 | 2026-08-12 | 1:1 交替、梯度隔离、一次共享前向、联合 checkpoint 和 CPU/CUDA 冒烟通过 |
| J3 | 通过 | 2026-08-13 | 10 轮联合训练完成；分类门槛合格，检测 val mAP@0.5:0.95 最高为 epoch 10；未读取 test |
| J4 | 通过 | 2026-08-13 | 两个冻结 test 各单遍评估一次；唯一联合模型包、严格重载和 checksum 通过 |
| J5 | 通过 | 2026-08-13 | 单权重联合应用、真实 API、CPU/CUDA、桌面和移动浏览器验收通过 |
| F1 | 通过 | 2026-08-13 | 版本链、唯一模型包、一次性 test、应用、文档和 Git 范围全部通过 |

独立 A0 已取消，其有效检查改为 A1 的无状态 preflight。历史退回记录保留在工作日志，不再作为活动门禁。

## T0 IP102 数据合同

- [x] 官方 trainval/test 分别为 15,178/3,798，互不重叠，test 原样保留。
- [x] 5 张正式划分外 JPEG 被记录并排除，正式 split 均有图片和 XML。
- [x] 97 个 IP102 源标签映射到 96 个检测类，并输出 DLCPD-25 `class_id 0-202`。
- [x] 重复根 XML 去重；唯一零宽框过滤；原始 XML 和图片未改。
- [x] train/val 只从官方 trainval 固定生成，三 split 全量 Dataset 遍历通过。
- [x] 记录每类图片/框分布、随机种子、算法、异常和 checksum。

## J1-J4 联合训练与评估

- [x] J1 从历史分类 `best.pt` 初始化，并用 DLCPD-25 train/val 适配统一 224 直缩；只读取 train/val，未读取 test。
- [x] 分类和检测使用同一 224 张量、同一共享主干和一次主干前向。
- [x] J2 以两个独立 DataLoader 按 1:1 step 比例交替训练。
- [x] 分类 step 不更新检测头，检测 step 不更新分类头；两种 step 更新共享主干。
- [x] 检测头为 96 个前景类加背景，内部标签不暴露到应用。
- [x] AMP、完整 checkpoint 重载、双任务小样本过拟合、CPU/CUDA 和显存冒烟通过。
- [x] J3 同时报告两个 val；分类下降门槛合格后按检测 mAP 选型。
- [x] J4 冻结后分类/检测 test 各执行一次，并记录无检测 test 支持类别。
- [x] 唯一联合模型包包含全部参数、配置、预处理、映射、双任务指标和 checksum。

## J5-F1 联合应用与最终验收

- [x] 应用只加载一个 checkpoint，一次模型调用同时产生分类和检测结果。
- [x] 输入统一直缩 224，检测框准确反算到原图。
- [x] 检测框类别统一使用 DLCPD-25 `class_id 0-202`。
- [x] 无框、多框、低置信度、损坏图和 CPU/CUDA 回退有测试。
- [x] 页面明确分类覆盖203类，检测只覆盖有IP102框的96类害虫。
- [x] 未伪造病害或缺陷框，未重新消费分类 test。
- [x] 全量测试、模型包 checksum、启动说明和演示路径通过。
- [x] Edge CDP 完成桌面与移动视口截图，确认布局、文字和检测图不重叠。

## 通用检查

- [x] 修改未超出角色职责，未覆盖用户已有工作。
- [x] 工程师按 7 行格式报告修改、命令、结果、指标和遗留问题。
- [x] 普通任务完成定向测试和 `git diff --check`。
- [x] A1、A3、P2、F0 完成项目全量测试。
- [x] 原图、taxonomy、固定 split 和冻结版本未被原地修改。
- [x] 当前阶段通过前未进入下一阶段。

## A1 训练链路

- [x] preflight 验证 D5 checksum、203 类、split 数量、路径和 duplicate group 契约。
- [x] Dataset 和 transform 使用固定 CSV 与 taxonomy，不从目录排序推断 class ID。
- [x] ResNet-50 输出为 `[N, 203]`，loss 和概率有限。
- [x] checkpoint 可重载，CPU/CUDA 冒烟通过。
- [x] 32-64 张固定小样本能够明显过拟合。
- [x] 全量测试通过。

## A2 完整训练

- [x] 使用 ImageNet 预训练、固定 seed 和固定 split。
- [x] 至少完成 ResNet-50 普通 CE 与一种不平衡策略的单变量对照。
- [x] 只用 val Macro-F1 选型，记录 Top-1、Top-5、Balanced Accuracy、时长和峰值显存。
- [x] 每个 run 保存解析配置、日志和可重载 checkpoint。
- [x] 未查看 test 指标进行调参；ConvNeXt-Tiny 仅在资源允许时对照。

## A3 评估与模型包

- [x] 先冻结模型、预处理和阈值，再执行一次 test。
- [x] 输出 Top-1、Top-5、Macro-F1、Balanced Accuracy、混淆矩阵和错误案例。
- [x] 模型包包含权重、manifest、配置、预处理、taxonomy、指标、模型卡和 checksums。
- [x] 固定样例推理稳定，taxonomy 或权重 hash 不匹配时拒绝加载。
- [x] 全量测试通过。

## P1 应用骨架

- [x] Predictor 契约、模型包 schema、Top-k 和三级 taxonomy 映射有测试。
- [x] 灰度、RGBA、EXIF、损坏图、超大图和模型缺失有明确处理。
- [x] Gradio 页面显示原图、宿主、四大类、细类、置信度、Top-5、版本和耗时。
- [x] 低置信度显示不确定提示，不绘制虚假检测框。

## P2 集成发布

- [x] A3 模型包 checksum 全部匹配后才加载。
- [x] 算法 CLI 和应用对固定样例的 class ID 与 Top-k 顺序一致。
- [x] CPU 可运行，CUDA 选择失败时合理回退或清晰报错。
- [x] 从仓库根目录按 README 可启动，包含 3-5 个演示样例和排错说明。
- [x] 全量测试通过。

## F0 最终验收

- [x] 数据、模型、应用版本和 Git commit 一致。
- [x] 安装、训练、评估、推理命令可以复现。
- [x] Git 中无原图、缓存、模型大文件或无关生成物。
- [x] F0 README 和答辩口径明确说明当时只完成图像分类；后续联合模型另按 T0、J1-J5、F1 验收。
- [x] 正常、低置信度和错误输入三条演示路径通过。
