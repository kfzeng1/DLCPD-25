# DLCPD-25 分类系统项目计划

## 1. 项目目标

在 20 个工作日内交付一个可答辩、可复现的 DLCPD-25 图像分类系统：用户上传一张图片，系统输出宿主作物、四大标签属性、具体病虫害/缺陷类别、置信度和 Top-5 结果。

项目不做目标检测。DLCPD-25 当前只有目录级图像标签，没有边界框、分割掩膜或严重度标注。

## 2. 标签层级

```text
宿主作物（22 个）
└── 标签属性（农业有害生物 / 植物病害 / 健康 / 非生物或生理缺陷）
    └── 具体类别（203 个）
```

训练的最终标签是 203 个具体类别。宿主和四大类从 `metadata/class-taxonomy.json` 读取，用于级联展示、分层评估和错误分析。

## 3. 硬件与模型决策

当前电脑：Intel Core i7-13700H、20 线程、15 GiB RAM、NVIDIA RTX 4070 Laptop，实际可用显存 7.62 GiB。已有 `/home/zkf/pytorch-env`，包含 Python 3.12.3、PyTorch 2.11.0+cu128 和 torchvision 0.26.0+cu128，CUDA 已实测可用。磁盘空间足够保存原图、缓存、模型和实验结果，但不应复制数据集。

主线采用 ImageNet 预训练的 `ConvNeXt-Tiny`，输入 224 x 224，AMP 混合精度，batch size 从 16 开始测试；显存不足时降到 8 或使用梯度累积。`ResNet-50` 作为稳定基线和故障回退。

不把 MAE、SimCLR v2、MoCo v3 作为首要交付：论文级自监督预训练需要更长训练时间和更大计算资源。它们只作为算法工程师在主线完成后的可选实验，优先使用公开预训练权重或在本地小规模抽样验证。

## 4. 交付物

| 交付物 | 验收标准 |
|---|---|
| 数据版本 | 203 类均存在，固定 class ID，去重后 train/val/test 不泄漏 |
| 分类模型 | 可加载权重，输入 RGB 图片输出 203 类 logits |
| 评估报告 | Top-1/Top-5、Macro-F1、Balanced Accuracy、混淆矩阵、宿主和四大类统计 |
| 推理应用 | 上传图片后显示三级结果和置信度；低置信度有提示 |
| 工程文档 | 环境、训练、评估、推理、复现实验命令完整 |
| 发布包 | 代码、配置、类别映射、模型校验和和结果表版本一致 |

## 5. 20 天节奏

| 时间 | 里程碑 | 负责人 |
|---|---|---|
| 第 1 天 | D0：冻结数据、类别和 taxonomy 口径 | 数据工程师 |
| 第 2 天 | D1：manifest、图片解码和坏图审计 | 数据工程师 |
| 第 3 天 | D2：SHA-256、dHash 和重复组 | 数据工程师 |
| 第 4 天 | D3：按重复组生成固定 split | 数据工程师 |
| 第 5 天 | D4-D5：复现测试、冻结 data-v1；P0：冻结推理契约 | 数据、应用工程师 |
| 第 6 天 | A0-A1：数据准入和训练冒烟；P1：假模型推理内核 | 算法、应用工程师 |
| 第 7-8 天 | A2：ResNet-50 基线；继续完成 P1 | 算法、应用工程师 |
| 第 9-11 天 | A3：ConvNeXt-Tiny 和不均衡对照；P2：假模型页面 | 算法、应用工程师 |
| 第 12-13 天 | A4-A5：冻结选型、一次最终测试和模型包 | 算法工程师 |
| 第 14-16 天 | P3：接入冻结模型包并完成端到端联调 | 应用、算法工程师 |
| 第 17-18 天 | P4：异常输入、低置信度、演示样例和发布资料 | 应用工程师 |
| 第 19 天 | F0：全量回归、README、报告、截图和环境锁定 | 总负责人 |
| 第 20 天 | 最终验收、打包、Git 提交和演示排练 | 总负责人、用户 |

详细阶段编号、用户调用模板和停点规则见 `workflow.md`；逐项通过标准见 `acceptance-checklist.md`。时间表允许 P0-P2 与算法工作交错，但 D0-D5、A0-A5 各自内部必须顺序执行，P3 必须等待 A5。

## 6. 阶段控制

- 用户每次只调用一位工程师完成一个阶段；工程师交付后立即停止。
- 总负责人检查前置条件、独立重跑测试并给出通过、退回或阻塞结论。
- 用户在收到验收总结后决定是否调用下一阶段。
- 计划日期不是通过证据；未在验收清单标记通过的阶段均视为未完成。
- 所有 Git 提交和推送由用户明确下令，总负责人检查提交范围。
- 表中日期是目标节奏，不是强制并行命令；若某阶段退回，优先修复门禁问题，再调整后续日期。

## 7. 风险控制

- 203 类长尾明显：不承诺每一类都达到相同精度；报告必须列出少样本类别。
- 近重复图片会造成虚高指标：先分组再切分，禁止先随机切分。
- 预训练权重下载失败：保留 ResNet-50 权重缓存和离线加载说明。
- 显存不足：优先降低 batch size，再降低输入尺寸，最后采用梯度累积；不随意改变类别映射。
- 低置信度图片不强行给出确定诊断，界面显示“可能不在已知类别内”。
