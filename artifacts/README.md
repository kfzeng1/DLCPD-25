# 运行产物目录

`artifacts/` 用于保存数据审计、数据划分和训练脚本生成的中间结果。除本说明外，该目录默认不进入 Git，原始数据也不存放在这里。

当前最终模型、指标和演示材料已经整理到 `project/assets/` 与 `project/docs/`。运行根目录下的数据或训练脚本时，可以继续将临时输出写入本目录；不同实验应使用独立子目录，避免覆盖已有结果。

需要长期保留的结果应同时保存配置、摘要和 SHA-256 校验文件。内容完全相同的大文件可以使用硬链接节省空间，但不要在原路径直接修改硬链接文件。
