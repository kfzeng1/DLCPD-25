# 最终模型权重

本目录在本地提交包中包含以下权重文件：

- `classification_best.pt`：DLCPD-25 分类专家 best checkpoint，约 428MB；
- `detection_best.pt`：IP102 检测专家 best checkpoint，约 696MB。

由于单文件超过 GitHub 100MB 限制，权重只随线下 `project/` 目录提交，不推送到公开仓库。
